# Tonearm — test-architecture task (self-contained brief)

You are picking up ONE task in `/Users/arley/github/parso-tonearm`. Everything you
need is in this file. Ignore other parso-* repos — they are done or out of scope.

## The goal (one sentence)

Carve tonearm's host-compilable logic into a `TonearmCore` Swift Package that the app
**depends on**, move the ~70 host-runnable tests to `swift test`, keep exactly ONE
simulator smoke test, and switch CI from a simulator unit-test run to `swift test`.

This is the exact pattern already shipped for **voxglass** — copy it. If you want a
worked reference, read `/Users/arley/github/parso-voxglass/{Package.swift,project.yml}`
and `parso-voxglass/VoxglassUITests/VoxglassUITests.swift`.

## Target architecture ("Option B", the user's explicit choice)

- **Logic lives in a host-compilable Swift Package** (`TonearmCore`) that the app
  *depends on* — NOT a test-only overlay, NOT duplicate compilation. Single source of
  truth, compiler-enforced view/logic boundary. Tested with `swift test` on the macOS host.
- **View code (SwiftUI/UIKit) stays in the app target.** Views are NOT automation-tested;
  the user eyeballs them.
- **Exactly ONE simulator smoke test**: launch → visit every tab → assert each renders
  without crashing. Nothing else runs on the simulator.
- **CI runs `swift test` ONLY.** No simulator tests in CI — the user finds simulators
  unreliable on GitHub Actions. The one smoke runs locally only. TestFlight/archive jobs
  stay unchanged except for their `needs:` gate.

## Hard constraints (do not violate)

- **Do the work on a NEW branch** off the current one (`live-activity-reliability`).
  Do not extract on `live-activity-reliability` directly.
- **Never** add a simulator test job to CI. CI = `swift test` (+ existing guards) only.
- **Do not touch** parso-radio-ios-app (out of scope). voxglass/zhuwen/workout are DONE.
- The user has **no local device**. On-device testing = you commit to `main` + push so CI
  builds a TestFlight build. So everything must actually compile or TestFlight fails.
- This repo commits its `.xcodeproj`. It's generated by **XcodeGen** from `project.yml`
  (explicit file lists, not synchronized groups). After any file add/remove or project.yml
  change, run `xcodegen generate` and commit the regenerated `Tonearm.xcodeproj`, or CI
  won't see the change.
- Commit trailer on every commit:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- Only commit/push to `main` when the user asks (the merge-and-push-for-TestFlight step).
  Ask before merging to main.

## What the repo looks like today

- **Monolith**, 122 Swift sources under `Sources/` (App, Art, Audio{/EQ,/Opus}, Data,
  DesignSystem, Domain, Features/*, IA, Intents, Pro, Remote/*, Share, Sync, Widgets).
- **72 test files** under `Tests/` (+ `Tests/Fixtures/` bundled as resources).
- **3 UITests** under `UITests/` (AddFolder, Tonearm, TonearmSmoke) — collapse to one.
- `project.yml` targets: `Tonearm` (app), `TonearmShareExtension`, `TonearmWidgetsExtension`,
  `TonearmTests` (bundle.unit-test, simulator), `TonearmUITests` (bundle.ui-testing).
- Package manager dep: **GRDB** (`from: 7.0.0`) via SwiftPM. There is **no Package.swift yet** —
  you create it.
- CI `.github/workflows/ios.yml`: job `test` runs `xcodebuild test -only-testing:TonearmTests`
  on `platform=iOS Simulator,name=iPhone 17 Pro`; `testflight-build` `needs: test`. There are
  two guard steps to KEEP: a StoreKit import-boundary guard and a product-name guard.

### Known UI-coupling to invert (expect the same seams as voxglass)

`grep` shows non-view Sources that import UIKit/SwiftUI (these block host compilation and
need protocol seams or `#if canImport(UIKit)`):
- `Sources/Audio/AudioPlayer.swift` (AVFoundation/MediaPlayer/AVAudioSession — like voxglass
  PlaybackCoordinator; use a platform-bridge seam, keep app-side `SystemPlaybackBridge` equiv)
- `Sources/Data/ArtworkService.swift`, `Sources/Data/ArtworkStore.swift` (UIImage — like
  voxglass ArtworkService; inject a Data-based artwork provider, keep decode app-side)
- `Sources/Data/SpectrogramDetector.swift` (UI-coupled)

Two tests are **NOT** host-runnable and must NOT move to `swift test` (they stay behind, or
their logic gets extracted): `Tests/SpectrogramDetectorTests.swift`,
`Tests/WidgetSnapshotTests.swift`. The other ~70 are host-runnable.

## Step-by-step (the voxglass playbook)

1. **Branch.** `git checkout -b tonearm-core-package` (off live-activity-reliability).
2. **Decide Core's contents.** Everything host-compilable that tests touch: Domain, Data
   (minus UIKit bits), Audio logic (minus AVFoundation platform calls), IA, Remote, Sync,
   Pro logic, Intents logic, EQ/Opus DSP. Views (Features/*, DesignSystem, App, Widgets)
   stay in the app.
3. **Invert UI coupling** with protocol seams (see voxglass: `PlaybackPlatformBridge`,
   `CoverArtworkValidating`, Data-artwork provider) and `#if canImport(UIKit)` where cheap.
   Keep audio/playback *logic* byte-for-byte; only move platform calls app-side.
4. **Create `Package.swift`** — a `TonearmCore` library target (path to the carved sources,
   add GRDB as a package dep + product dep, plus any `linkerSettings` like sqlite3 if needed)
   and a `TonearmCoreTests` test target (path `Tests`, `exclude` Fixtures/Info.plist as needed,
   depend on TonearmCore). Mirror voxglass's `Package.swift` shape.
5. **Access-level churn** (the big cost): everything the app/views consume goes `internal → public`.
   voxglass used an indentation-scoped `public`-izer script + hand-fixes. Pitfalls learned there:
   `public` is illegal on protocol requirements and on protocol-conformance extension bodies
   (mark the methods public instead, not the extension); synthesized memberwise inits are
   internal (add explicit `public init`); `nonisolated`/`private(set)`/nested-type members and
   attribute ordering (`@Published public var`, `@MainActor public final class`,
   `public nonisolated func`) all need care. There's a reusable script at
   `/private/tmp/.../scratchpad/publicize.py` from the voxglass session if it survived; otherwise
   rewrite it (rules in the voxglass memory).
6. **Point the app at the package.** In `project.yml`: add `packages: TonearmCore: {path: .}`,
   add `- package: TonearmCore` to the app target deps, and `excludes:` the carved dirs from the
   app's `sources`. Verify **0 Core files** remain in the app build phase after `xcodegen generate`.
7. **Repoint tests.** `@testable import Tonearm` → `@testable import TonearmCore` across the moved
   test files. Leave SpectrogramDetector/WidgetSnapshot tests out of `swift test` (delete them from
   the package test target, or keep their logic by extracting into Core).
8. **Remove the `TonearmTests` unit-test target** from project.yml + the scheme (redundant with
   swift test; also dodges the iOS-26 unsigned-injected-bundle load failure). Collapse `UITests` to
   ONE smoke `testAppBootsAndVisitsAllTabs` (launch, assert boot anchor, tap each tab button and
   assert a stable static-text anchor). Keep only `TonearmUITests` in the scheme's test targets.
9. **CI** (`.github/workflows/ios.yml`): replace the simulator "Run Unit Tests" step with a
   `swift test` run (or a separate `logic-tests` job on macos-latest). KEEP the StoreKit-boundary
   and product-name guard steps. Point `testflight-build`'s `needs:` at the new job(s). No
   simulator test job.
10. **Verify locally**, all three must pass:
    - `swift test` (host) — expect ~70 tests green
    - app builds: `xcodegen generate` then `xcodebuild build -project Tonearm.xcodeproj -scheme Tonearm -destination 'platform=iOS Simulator,name=iPhone 16'`
    - the one smoke passes on **iPhone 16** (UDID D81580B5-1110-4F0A-9AF4-8EAEAB259AF9 — the only
      simulator on this Mac; iOS 26.5)
11. **Commit** on the branch. Then **ask the user** before merging to `main` + pushing (that's the
    step that builds their TestFlight build for on-device regression).

## Simulator facts

- Exactly one sim exists: **iPhone 16, iOS 26.5**, UDID `D81580B5-1110-4F0A-9AF4-8EAEAB259AF9`.
  Always target `platform=iOS Simulator,name=iPhone 16` locally. (CI's old `iPhone 17 Pro`
  destination goes away when you switch CI to `swift test`.)
- Xcode 26 / iOS 26.5 SDK; `xcrun simctl`.

## Done means

`swift test` green on host (~70 tests), app links `TonearmCore` (0 Core files compiled by the
app), one passing simulator smoke, CI runs `swift test` only (guards kept), everything on a
branch — then merge+push to main on the user's go for a TestFlight build. After it's merged,
update the memory file `/Users/arley/.claude/projects/-Users-arley-github/memory/parso-test-architecture.md`
(the `TODO tonearm` bullet → DONE) and its `MEMORY.md` pointer.
