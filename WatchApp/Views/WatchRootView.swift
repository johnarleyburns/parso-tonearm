import SwiftUI
import TonearmCore

struct WatchRootView: View {
    var body: some View {
        List {
            Text("Platterhead")
                .font(.headline)
            Text("Watch app scaffolding — Phase 2")
                .font(.caption2)
                .foregroundStyle(.secondary)
        }
    }
}
