import Foundation
import TonearmCore

enum WatchFixtureSeeder {
    static func seed() {
        // Phase 6 will seed the watch LibraryStore with fixture content
        // for the smoke test. For now, this is a no-op placeholder.
    }
}
